# Human-readable module metadata.
# ABK reads this file to validate and prefill module options.

ABK_MODULE_NAME="ABK Kasumi"
ABK_MODULE_VERSION="1.0.0"
ABK_MODULE_DESCRIPTION="Kasumi - a kernel-level path manipulation and hiding framework."
ABK_MODULE_SUPPORTED_STAGES="after_patch,before_build"
ABK_MODULE_DEFAULT_STAGE="after_patch"
ABK_MODULE_RECOMMENDED_STAGES="after_patch,before_build"

# Strict mode fails the build when a targeted rule remains after cleanup.
# ABK_DIRTY_SEPOLICY_STRICT="${ABK_DIRTY_SEPOLICY_STRICT:-1}"
